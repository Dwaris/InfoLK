# Eingabe
groesse = float(input("Deine Größe  (in m) : "))
gewicht = float(input("Dein Gewicht (in kg): "))

# Verarbeitung
bmi = gewicht / (groesse * groesse)

# Ausgabe
print("Dein BMI beträgt", bmi)
